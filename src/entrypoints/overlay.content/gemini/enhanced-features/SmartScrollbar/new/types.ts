/**
 * Re-export shared types and utilities from parent.
 * The new scrollbar uses the same data model.
 */
export type { ConversationNode } from '../types';
export { extractHeadings } from '../types';
