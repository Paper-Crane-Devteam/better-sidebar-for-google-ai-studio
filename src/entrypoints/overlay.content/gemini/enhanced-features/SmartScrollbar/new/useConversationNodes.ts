/**
 * Re-export the conversation nodes hook from the parent directory.
 * The data layer remains unchanged — only the UI is rewritten.
 */
export { useConversationNodes } from '../useConversationNodes';
export type { ConversationNode } from '../types';
