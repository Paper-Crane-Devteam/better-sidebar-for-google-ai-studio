/**
 * Re-exported from the shared component so the library tree and the folder
 * picker highlight matches with exactly the same markup.
 */
export { HighlightedText } from '@/shared/components/HighlightedText';
